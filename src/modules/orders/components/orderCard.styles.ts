/**
 * Единый визуальный стиль карточек-блоков заказа (bento-подход). Держим в одном
 * месте, чтобы блоки на странице успеха и будущей странице заказов совпадали.
 */
export const ORDER_CARD_CLASS =
	"rounded-[var(--radius-lg)] border border-[var(--border-light)] bg-[var(--surface)]";

/** Заголовок секции внутри карточки. */
export const ORDER_CARD_LABEL_CLASS =
	"text-xs font-medium uppercase tracking-wider text-[var(--text-secondary)]";
