/**
 * Ключи React Query модуля баннеров.
 *
 * Ключ один: «следующий баннер для меня». Страница в него НЕ входит, хотя
 * уходит в запрос, — иначе каждый переход по сайту создавал бы новый ключ,
 * новый кэш и новый запрос, то есть превращал навигацию в опрос. Когда
 * переспросить из-за смены экрана, решает флаг `pageBlocked` в ответе.
 */
export const bannersKeys = {
	all: ["banners"] as const,
	next: () => [...bannersKeys.all, "next"] as const,
};
