// import { $convertToPlainText } from '@payloadcms/richtext-lexical'

// export function extractPlainText(node: any): string {
//   if (!node || !node.root) return ''
//   // $convertToPlainText ожидает EditorState, но для простого JSON можно вызвать напрямую с root
//   return $convertToPlainText(node.root) ?? ''
// }