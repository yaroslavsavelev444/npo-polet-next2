/** Минимальная длина слова, которое имеет смысл искать. */
export const SEARCH_MIN_TERM_LENGTH = 2;

/** Сколько слов запроса учитываем. Всё сверх — шум и лишние условия в SQL. */
export const SEARCH_MAX_TERMS = 6;

/** Результатов на странице выдачи. */
export const SEARCH_PAGE_SIZE = 12;

/**
 * Потолок выборки для обзорного дерева /knowledge. Та же логика, что и выше:
 * дерево собирается в памяти одним проходом по облегчённой проекции.
 */
export const OVERVIEW_TOPIC_LIMIT = 1000;

/** Сколько статей показываем в группе до кнопки «показать все». */
export const GROUP_PREVIEW_SIZE = 5;

/** Максимум рекомендованных статей под материалом. */
export const RELATED_LIMIT = 6;

/** Длина фрагмента текста в результатах поиска. */
export const SNIPPET_LENGTH = 180;

/**
 * Граница между «шапкой» материала (заголовок, описание, теги) и телом статьи
 * внутри одного поля searchText.
 *
 * Нужна выдаче поиска: фрагмент под названием материала должен показывать
 * место из ТЕКСТА статьи, а не повторять заголовок и описание, напечатанные
 * прямо над ним. Делить по длине заголовка ненадёжно (нормализация меняет
 * строку), а заводить вторую колонку ради этого — платить дублированием всего
 * текста статьи.
 *
 * Символ-разделитель непечатаемый и не является словом, поэтому to_tsvector не
 * порождает из него ни одной лексемы: полнотекстовый поиск его не видит.
 */
export const SEARCH_BODY_SEPARATOR = "\u0007";
